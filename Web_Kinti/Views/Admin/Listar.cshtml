@model IEnumerable<Web_Kinti.Models.Usuario>

<html>
<head>

    <link rel="stylesheet" href="~/css/AdminMenu.css">

    <link rel="stylesheet"
          href="https://unpkg.com/boxicons@latest/css/boxicons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.2.0/fonts/remixicon.css" rel="stylesheet">

</head>
<body>
    <header>
        <a href="#" class="logo">KIN<span>TI</span></a>

        <ul class="navbar">
            <li><a href="registraru">Registrar usuario</a></li>
            <li><a href="Listarpro">Menú</a></li>
            <li><a href="listarre">Reservaciones</a></li>
        </ul>

        <div class="h-right">
            <a href="#">Siguenos</a>
            <a href="#"><i class="ri-instagram-fill"></i></a>
            <a href="#"><i class="ri-twitter-fill"></i></a>
            <a href="#"><i class="ri-facebook-fill"></i></a>
            <div class="bx bx-menu" id="menu-icon"></div>
        </div>
    </header>

    <div class="table">

        <div class="table_section">
            <table>
                <thead>
                    <tr>
                        <th scope="col">Codigo</th>
                        <th scope="col">Nombre</th>
                        <th scope="col">Apellido</th>
                        <th scope="col">Teléfono</th>
                        <th scope="col">Foto de perfil</th>
                        <th scope="col">Rol</th>
                        <th scope="col">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.IdUsuario</td>
                            <td>@item.Nombre</td>
                            <td>@item.Apellido</td>
                            <td>@item.Telefono</td>
                            <td><img src="@item.UrlFoto"></td>
                            <td>@item.IdRol</td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.IdUsuario">
                                    <button class="update-button"><i class='bx bxs-edit'></i></button>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.IdUsuario">
                                    <button class="delete-button"><i class='bx bxs-trash-alt'></i></button>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>